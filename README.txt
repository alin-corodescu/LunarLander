Dependencies of this project :
    OpenAI gym : pip install gym
    box2d : pip install box2d (might need to install swig as well - sudo pacman -S swig)
            pip3 install box2d box2d-kengz (the second package is needed to fix an issue)
    Keras
    Theano
    h5py